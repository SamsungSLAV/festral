insert("device_type: ".%target);
insert("job_name: ".%target."-test");
raw(
priority: medium

actions:
  - boot:
      login: login
      password: password
  - test:
);
insert("      name: ".%repo."-".%name."-".%target);
raw(
      test_cases:
);
insert("        - case_name: ".%name);
raw(
          test_actions:
);
